function windowopen(url){    window.location.href = url;}function openWindow(url, i){    if(parentArr[i]){        try{parentArr[i].close();}catch(e){}    }    var openobj = window.open(url);    parentArr[i] = openobj;}var parentArr = [];function closeWindow(){    for(var i = 0; i < parentArr.length; i++){        if(parentArr[i]!=undefined && parentArr[i]!=null){            try{parentArr[i].close();}catch(e){}        }    }    window.location.href = "../login.do?action=relogin";}window.onload = function(){    createWelcome();}var welcomeTitle = '';function createWelcome(){    var userobj;    var url = rootpath + '/wkmh/loadData.do';    var param = {        action: 'getUserInfo',        sysAccessID: accessid    };    $.ajax({        url: url,        data: param,        method: 'POST',        async: false,        success: function (resultXml) {            userobj = eval("(" + resultXml + ")");        },        error: function (e) {            alert(e);        }    });    var obj = userobj.onlineUsers[0];    var username = obj.username;    var timmer = "";    var myDate = new Date();    var hours = myDate.getHours();    if(hours <12 && hours>=5 ){        timmer = "上午";    }else if(hours>=12 && hours<14){        timmer = "中午";    }else if(hours>=14 && hours<19){        timmer = "下午";    }else if(hours>=19 || hours ==0 || hours<5){        timmer = "晚上";    }    welcomeTitle = timmer + "好，" + username;    document.getElementById("welcome").innerHTML = welcomeTitle;}